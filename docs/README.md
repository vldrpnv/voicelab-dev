# Documentation map

- `contexts/` — initial product, domain, architecture, and constraint context already present on `main`.
- `adr/` — global architectural decisions for repository-wide concerns.
- `requirements/` — versioned requirements and structure-level expectations.
- `discussions/` — unresolved items, challenge questions, and notes that are not yet promoted to decisions.
