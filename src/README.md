# Assistant-core prototype

`src/` remains the smallest viable home for the current assistant-core prototype. Move code out into more specific component directories only when a concrete service, integration, or package boundary is ready.
